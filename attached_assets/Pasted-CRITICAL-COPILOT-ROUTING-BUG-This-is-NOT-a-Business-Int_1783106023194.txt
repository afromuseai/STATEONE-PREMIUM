CRITICAL COPILOT ROUTING BUG

This is NOT a Business Intelligence generation issue.

The generation command is being emitted BEFORE the target module exists.

Evidence from runtime logs:

ROUTING_TRACE_CLIENT
command: generate_intelligence
currentLocation: /dashboard

Workspace signal:

target: intelligence
type: generate

mounted: false
subscribers: 0

This proves:

Business Intelligence never mounted.

The generation signal is emitted into zero subscribers.

The signal is lost.

==================================================

EXPECTED FLOW

Marcus confirmation

↓

Navigate to Business Intelligence

↓

Business Intelligence mounts

↓

Workspace controller registers

↓

Subscriber attaches

↓

Only THEN emit generation

↓

Generation starts

==================================================

Audit ALL generate_* commands.

Specifically investigate:

generate_intelligence

generate_chatbot

generate_automation

generate_orchestrator

Verify each follows the Website lifecycle.

==================================================

Website is the reference implementation.

Do NOT emit generation while:

mounted == false

OR

subscribers == 0

Generation must wait until the destination module is fully mounted.

==================================================

Also investigate Marcus confirmation responses.

After confirmation Marcus currently responds:

"Generating now."

This appears to be hardcoded rather than produced by the LLM.

Trace:

detectConfirmationIntent()

confirmationEngine

executeConfirmedAction()

GENERATE_CMD_MAP

Any confirmation shortcut responses.

Determine whether the confirmation path bypasses the LLM.

If it does:

Restore normal LLM generation.

Marcus should produce contextual responses, not fixed template strings.

==================================================

Required output:

1. Exact file where generate_intelligence is emitted.
2. Why navigation is skipped.
3. Why mounted=false and subscribers=0.
4. Why Website avoids this problem.
5. Whether the confirmation response is hardcoded or generated by the LLM.
6. Proposed fix before modifying any code.